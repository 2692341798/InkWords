# 墨言知识训练平台 (InkWords Trainer) - 产品需求文档 (PRD)

## 0. 变更记录
- 2026-06-05：继续推进 `core-api / llm-stream` 深拆分的内部 bridge 收口。本次不新增用户可见功能，也不改变 `http://localhost` 单入口、SSE 或页面交互；仅要求 service 内部优先删除已经没有外部调用价值的 `generator` 与 `continue` 桥接层，为后续继续收口 `series / Chapter` 兼容层做准备。
- 2026-06-05：继续推进 `core-api / llm-stream` 深拆分的内部契约收口。本次不新增用户可见功能，也不改变 `http://localhost` 单入口、SSE 或任何页面交互；仅要求非 `service` 域逐步绕过兼容别名，直接依赖 `domain/blog/contracts`，为后续删除 service 兼容桥接层做准备。
- 2026-06-04：新增 `FRONTEND_PORT` 作为本地 Docker Compose 前端端口覆盖能力。本次不新增用户可见功能，也不改变默认产品入口 `http://localhost`；仅允许在宿主机 `:80` 被其它进程占用时，将前端入口临时切到如 `http://localhost:8088`，以便继续完成本地验证与演示。
- 2026-06-04：Generation Task-Only Task 4 打通 `generate_series` 的后台持久化闭环。本次不新增用户可见功能，也不改变系列生成入口、SSE 或页面交互；仅要求系统在 `generate_series` 任务成功后，由 `core-api` 自动消费结构化 `result_json` 并把系列父博客导读与章节终态/正文落回业务表，同时继续记录 token 记账。
- 2026-06-04：Generation Task-Only Task 3 打通 `continue` 的后台持久化闭环。本次不新增用户可见功能，也不改变续写入口、SSE 或页面交互；仅要求系统在 `continue` 任务成功后，由 `core-api` 自动消费结构化 `result_json` 并把 `final_content` 更新回博客正文，同时继续记录 token 记账。
- 2026-06-04：Generation Task-Only Task 2 打通单篇任务结果的后台持久化闭环。本次不新增用户可见功能，也不改变任务创建、SSE 或页面交互；仅要求系统在 `generate_single` 任务成功后，由 `core-api` 自动消费结构化 `result_json` 并把最终正文与 token 记账落回业务表，为后续继续把 `continue / generate_series` 纳入同一闭环做铺垫。
- 2026-06-04：Generation Task-Only Task 1 落地单篇生成结果 contract。本次不新增用户可见功能，也不改变 `http://localhost` 单入口、任务创建/SSE 交互或最终博客展示；仅要求后端在 `generate_single + INKWORDS_TASK_PERSISTENCE_MODE=task_only` 下把任务成功结果升级为结构化 `result_json`，为后续由 `core-api` 接管最终业务落库做准备。
- 2026-06-04：Phase 2 执行 `core-api / llm-stream` 深层拆分第一轮。本次不新增用户可见产品功能，但把“生成任务由 `core-api` 负责业务事实落库、`llm-stream` 聚焦流式执行与任务表写入”正式收口为产品运行约束；系统仍保持 `http://localhost` 单入口、`/api/*` 路径不变，并保留旧 `/api/v1/stream/*` 与 `/api/v1/blogs/:id/(continue|polish)` 作为紧急回滚路径。
- 2026-06-04：Task 4 完成 `export-service` 代码目录归属迁移。`export-service` 的启动装配、私有路由、consumer 与导出产物 store 迁入 `backend/services/export-service/`；本次不新增产品功能，不改变导出链路、对外 API、任务交互或数据库结构。
- 2026-06-03：任务中心从“仅生成链路任务化”扩展到“生成 + 解析 + PDF 导出”三类后台任务。系统在保持 `http://localhost` 单入口不变的前提下，新增解析任务创建、PDF 导出任务创建与导出文件受控下载能力；其中 `.zip` 课件包与 `50MB` 以上普通单文件默认走任务式解析，系列 PDF 导出改为“创建任务 -> 等待完成 -> 下载产物”。
- 2026-06-03：生成链路升级为“任务式 SSE 第一阶段”。在不改变 `http://localhost` 单入口与 `/api/*` 对外路径的前提下，新增 RabbitMQ 支撑的生成任务流：前端生成时需先创建任务，再订阅任务流；后端需提供任务创建、查询、取消与 SSE 订阅接口，并由生成服务异步消费任务、持续回写状态与正文 chunk。当前阶段仅覆盖生成链路，不要求 parser/export/review 同步任务化。
- 2026-06-03：稳定性与工程化优化（Task 1-5）。本次不新增产品功能；主要补齐非功能性能力：后端服务支持优雅停机与请求取消传播（客户端断开后默认停止后台任务），系列生成补齐事务边界并避免 Token 记账静默失败；前端对流式输出引入批量刷新降低卡顿，编辑器预览使用 deferred 降低频繁重渲染成本，鉴权状态改为响应式并清理阻塞式弹窗与强制刷新；容器化启动方式收口为 `docker compose --env-file backend/.env ...` 并新增 `.env.example`、PR 模板与 CI 门禁。
- 2026-06-02：系列生成失败时，前端进度面板与侧边栏任务区不能只显示笼统的 `Error`；系统必须直接展示章节或系列导读的中文失败原因，并在章节重试或重新生成后自动清除旧错误提示。同时，后端流式写出链路需要增强缓冲与主动刷新，降低慢客户端下的流式背压风险。
- 2026-06-01：文件来源 Analyze 新增“内容类型识别 + 动态提示词类型锁定”要求；系统必须在生成大纲前识别当前文件更适合哪类提示词（如心理学经典解读、技术资料讲解、开卷复习材料），在大纲区展示“当前提示词类型”只读标签，并要求后续正文生成沿用同一次 Analyze 已锁定的提示词类型。
- 2026-06-01：知识漫游复习升级为“按文章内容结构化追问 + 明确命中/遗漏反馈”；创建/恢复会话时必须返回当前文章的复习快照与本轮目标，回答后必须明确告诉用户“答到了什么、漏了什么、下一步补什么”。
- 2026-06-01：知识漫游复习推荐卡必须提供显式 `提问开始` 入口；当当前题卡不支持 `detailed_qa` 时，该入口必须禁用。首页“继续上次任务”不得把 `completed` 的复习会话继续渲染成可恢复任务。
- 2026-05-29：收敛“知识漫游复习”入口，移除与随机抽题职责重复的“今日推荐”卡片；当前主入口保留“随机抽题 + 手动选文”，并要求“再抽一篇”能够实际换题。
- 2026-05-29：生成器流程需求调整为“三步 + 步内进度”：顶部仅保留 `选择来源 / 配置解析 / 确认大纲` 三步；解析/分析进度必须内嵌在“配置解析”，写作进度必须内嵌在“确认大纲”，`处理进度` 不再作为独立步骤。文件上传路径明确为“先 parse、后在配置页显式触发 analyze”，不得在上传成功后自动跳过场景选择。
- 2026-05-28：工程规范收尾与提交前同步：本次不新增产品功能，但补强了若干交付约束，要求前端展示层在核心登录/仪表盘链路中保持中文文案一致性，复杂 Hook/组件补齐说明性注释，并将容器化运行约定统一为通过 `docker compose --env-file backend/.env ...` 启动，且默认仅以前端 `http://localhost` 作为公开访问入口。
- 2026-05-27：项目定位升级为“墨言知识训练平台（InkWords Trainer）”，口号“把资料变成知识，把知识变成能力”；主叙事聚焦知识沉淀与复习闭环，博客生成为可选输出能力。
- 2026-05-27：新增“流程型工作入口”需求；登录后的默认工作入口应优先展示 `HomeEntry` 决策中心，用户一次只聚焦一个主步骤，首页保留“继续上次任务/最近记录”，并在首页预览与页内进度中复用统一的 `StepStrip` 视觉语义。
- 2026-05-27：新增“知识漫游复习”能力，要求在独立工作台内支持今日推荐、随机抽题、手动选文、两种训练模式、最近记录与中文反馈闭环。
- 2026-05-25：将本地文件上传能力上限从 100MB 提升到 888MB，要求前端校验、网关层和后端解析层保持统一。
- 2026-05-25：新增“正文纯净输出”约束；无论是首次生成、继续生成还是全文润色，用户最终看到和保存的正文都不得包含 AI 思考标签、推理过程或“好的，收到你的需求 / 作为高级全栈架构师”等对话式前言。
- 2026-05-25：明确“创作场景”只允许在大纲生成前切换；Analyze 返回大纲后，前端必须隐藏场景选择器并展示当前场景的只读标签，保证大纲与后续正文生成使用同一场景语义。
- 2026-05-25：修复电子书解读系列在异常场景下历史博客只显示导读的问题；系列章节即使生成失败也必须保留子节点记录，确保历史列表和后续重试具备稳定的系列结构。
- 2026-05-24：新增“创作场景（`scenario_mode`）”能力，支持电子书解读、开卷复习、小白教程三类任务目标，并贯通分析与生成链路。
- 2026-05-21：修复本地文档上传后被误判为 Git 解析来源的问题（Bugfix，不涉及 PRD 需求变更）。
- 2026-05-21：本地文件解析新增 ZIP 课件包支持；可自动扫描 ZIP 内文档/代码文本文件，完成去重与顺序聚合后进入既有分析链路。
- 2026-04-29：代码模块化整理（不涉及 PRD 需求变更）。
- 2026-04-29：新增“手写博客入口”（创建草稿并进入编辑器）。
- 2026-05-08：写博客编辑器新增“语音输入”（浏览器实时转写）。
- 2026-05-08：写博客编辑器新增“润色”（流式生成润色草稿 + 一键应用到正文），并优化 Markdown 预览标题/表格排版。
- 2026-05-08：工程化整理（不涉及 PRD 需求变更，主要为仓库文件治理与文档同步）。
- 2026-05-08：目录结构工程化调整落地（工程结构调整，不涉及 PRD 需求变更）。
- 2026-05-08：后端 Blog Domain 垂直切片迁移（工程结构调整，不涉及 PRD 需求变更）。
- 2026-05-08：后端 User Domain 垂直切片迁移（工程结构调整，不涉及 PRD 需求变更）。
- 2026-05-08：后端 Auth Domain 垂直切片迁移（工程结构调整，不涉及 PRD 需求变更）。
- 2026-05-08：后端 Stream/Project Domain 垂直切片迁移（工程结构调整，不涉及 PRD 需求变更）。
- 2026-05-10：修复导出到 Obsidian 初始化 scaffold 时的目录列表解析失败（Bugfix，不涉及 PRD 需求变更）。
- 2026-05-10：新增“文章类型/风格选择”与“写作要求模板管理”（生成器可选风格；编辑器可管理模板）。
## 1. 产品定位
墨言知识训练平台 (InkWords Trainer) 是一款面向个人知识沉淀与复习的本地知识工作台。它能够将本地文档（PDF/DOCX/Markdown/TXT/ZIP 课件包）或开源 Git 仓库（代码/官方教程）整理为可持续增长的 Obsidian 知识库，并通过“知识漫游复习”形成训练闭环；同时保留将内容进一步生成小白友好、可复现的系列技术博客作为可选输出能力。

## 2. 核心功能需求

### 2.1 文档解析与生成
- **本地文件解析**：用户可上传 PDF/DOCX/Markdown/TXT 格式的技术文档，系统提取文本内容，单文件最大支持 888MB，并在前端与网关层进行统一拦截校验。针对大于 5 万字符的长文档，系统将自动进行分块（Map-Reduce）分析。
- **ZIP 课件包解析**：用户可上传单个 ZIP 课件包；系统会自动扫描压缩包中的受支持文档与代码文本文件，完成白名单筛选、内容去重、按顺序聚合，并输出解析摘要（扫描数、保留数、去重数、忽略数、失败数）后继续进入大纲分析。
- **任务式解析扩展（2026-06-03 更新）**：
  - `.zip` 课件包与 `50MB` 以上普通单文件默认不再绑定同步长请求；前端必须先创建解析任务，再根据任务状态获取解析结果。
  - `50MB` 及以下普通单文件可继续走同步解析，作为兼容与回滚路径。
- **Git 仓库解析与模块选择**：用户输入公开的 Git 仓库地址，系统极速拉取目录结构并智能扫描一级核心代码目录，结合仓库 `README` 调用轻量级 AI 智能推断每个目录的作用并生成中文简介，以卡片形式展示。用户可按需勾选模块进行定向解析，加速处理并避免内容超限。
- **大纲拆解与规模评估**：系统根据解析到的长文本（选中的 Git 模块或超大本地文件），智能评估内容规模，并**强制拆分为系列博客大纲**（包含章节标题、摘要、排序等）。
- **内容生成与系列串联**：
  - 基于生成的大纲，系统以**并发**的方式调用 DeepSeek API 为每个章节生成具体的独立博客内容。
  - 支持“创作场景”选择：`电子书解读 / 开卷复习 / 小白教程`，用于表达本次任务目标；并继续支持“文章类型/风格”选择（如通用技术博客/小白手把手/备考复习）作为呈现风格层。
  - 对于文件上传来源，系统必须在 Analyze 阶段自动识别内容类型，并锁定匹配的动态提示词 profile；例如沟通/心理学书籍优先走“心理学经典解读”，考试材料优先走“开卷复习材料”，技术手册优先走“技术资料讲解”。
  - 所有独立博客生成完毕后，系统将自动触发一次 AI 调用，生成一篇全局的“导读/总结”文章，将单篇博客串联为完整的专栏系列。
  - **单点聚焦与深度重写**：强制单篇博客仅深入剖析一个核心技术点。对于源码更新后的已有章节，系统将启动**松散参考重写 (Regeneration)**，提取旧版博客注入大模型，基于最新源码保留优质解释并更新代码逻辑。
  - 生成的内容必须包含代码级剖析（逐行解释）和可复现的步骤。
  - 对于抽象的理论概念，必须提供对应的代码示例或生活化比喻（小白友好）。
  - 支持 Mermaid 图表（强制要求无自定义样式）。
- **实时进度展示**：前端需提供基于 Server-Sent Events (SSE) 的多卡片流式进度展示，支持用户实时预览多个并发章节的生成状态和打字机效果。对于系列章节质量流水线，至少要把 `理解章节 / 生成草稿 / 技术审稿 / 定向补强 / 输出终稿 / 已完成 / 失败` 以中文状态暴露给用户。
- **任务式生成链路（2026-06-03 更新）**：
  - 生成请求不再要求浏览器直接把长请求绑在 `/api/v1/stream/generate` 上；前端必须先调用任务创建接口，再订阅返回的 `stream_url`。
  - 系统必须提供 `任务创建 / 任务查询 / 任务取消 / 任务 SSE 订阅` 四类接口，以保证浏览器断连、刷新或后续恢复场景下，生成任务与前端连接可以解耦。
  - 生成服务必须支持异步消费任务、持续回写任务状态、正文 chunk 和完成结果；前端则以任务流为准更新章节状态、正文缓冲和完成态。
  - 当前第一阶段只要求“生成链路任务化”；文档解析、导出和知识复习仍可保留既有同步路径，后续再拆分迭代。
- **缓存命中可观测性**：系列章节在终稿补强完成后，系统必须额外返回该章节的 DeepSeek usage 与 Prompt Cache 命中统计，让后续前端能够展示“缓存命中 / 未命中”摘要，并用于评估稳定前缀策略是否有效。
- **提示词类型可见性**：当文件 Analyze 完成后，前端必须在大纲区展示当前已锁定的“提示词类型”中文标签与分类原因，让用户知道系统为什么把当前资料当作某一类内容来解释。
- **生成器步骤约束（2026-05-29 更新）**：
  - 顶部流程固定三步：`选择来源 -> 配置解析 -> 确认大纲`。
  - 解析/分析期间仍归属“配置解析”步骤，不得切换到独立“处理进度”页面。
  - 写作生成期间仍归属“确认大纲”步骤，不得切换到独立“处理进度”页面。
  - 上传 ZIP/文档后必须先停留在“配置解析”让用户选择创作场景，再由用户显式点击“生成大纲”触发分析。
- **正文纯净输出**：所有 AI 生成正文必须对用户可见内容做净化，严禁将模型思考标签、推理过程或对话式前言混入最终 Markdown 正文与保存结果。

### 2.1.1 创作场景需求（`scenario_mode`）
- **电子书解读**：适合长文档、经典著作、概念材料。输出应更偏向篇章结构、观点提炼、白话解释与现实映射。
- **开卷复习**：适合课件、实验指导、考试范围资料。输出应更偏向考点清单、步骤模板、易错点和速查结构。
- **小白教程**：适合 Git 仓库、项目教程、官方文档。输出应更偏向环境准备、目录结构、主链路、排错说明和可复现步骤。
- **默认策略**：
  - Git 仓库默认推荐“小白教程”。
  - 文件上传默认推荐“电子书解读”。
  - 用户可以在生成器页面显式手动切换，不应被系统默认值锁死。
- **交互约束**：
  - 用户只能在生成大纲（Analyze）之前手动切换 `scenario_mode`。
  - Analyze 返回大纲后，前端必须隐藏场景选择器，并在大纲区展示当前场景的只读中文标签。
  - 大纲生成后的正文生成必须沿用同一次 Analyze 已锁定的 `scenario_mode`，不允许中途修改。
- **兼容要求**：
  - 旧客户端不传 `scenario_mode` 时，后端必须自动兜底并保持链路可用。
  - `scenario_mode` 必须同时影响 Analyze 与 Generate，避免大纲和正文风格割裂。

### 2.1.2 动态提示词类型需求（`prompt_profile`）
- **目标**：在“创作场景”之外，系统还必须根据文件内容本身识别更细粒度的内容类型，用于决定系统角色、解读重点与正文组织方式。
- **适用范围**：
  - 仅对文件上传来源强制启用（PDF/DOCX/Markdown/TXT/ZIP 聚合文本）。
  - Git 仓库链路当前仍以 `scenario_mode + article_style` 为主，不要求先做内容类型分类。
- **最小能力要求**：
  - Analyze 前或 Analyze 过程中，系统必须对文件内容做一次轻量分类。
  - Analyze 结果必须返回 `prompt_profile_key`、中文展示名、`document_kind` 与分类原因。
  - Generate（单篇、系列章节、系列导读）必须沿用同一次 Analyze 已锁定的 profile，不能在生成时重新漂移到其它提示词类型。
- **回退要求**：
  - 当分类器不可用、内容为空、返回非法值或解析失败时，系统必须按 `scenario_mode` 回退到默认 profile，并显式告诉用户当前为默认提示词。
- **前端交互约束**：
  - 大纲出现后，用户可以看到“当前创作场景”和“当前提示词类型”两个只读标签。
  - 当前版本不要求用户手动切换 prompt profile，默认由系统自动识别并锁定，以减少对小白用户的额外认知负担。

### 2.2 博客管理
- **历史记录列表**：用户可查看所有已生成的博客（按系列父节点和子章节组织）；当系列章节生成中断或局部失败时，系统仍需保留系列父子结构，避免历史区退化为仅显示导读。
- **流程型工作入口**：当用户未打开具体博客编辑器时，系统默认先进入引导式 `HomeEntry`，而不是直接把用户扔到生成器表单；首页需要能在“生成博客 / 知识复习”两类主路径之间切换，并保留继续任务与最近记录。
- **Markdown 编辑器**：内置 Markdown 编辑器，支持用户对生成的博客进行二次编辑与实时预览，并在输入停止后自动保存到数据库。
- **语音输入**：在写博客编辑器中支持语音转写输入，转写内容实时写入正文，提升手写写作效率。
- **润色**：支持对当前草稿进行全文润色，前端展示“润色预览”，并支持一键将润色结果应用到正文。
- **手写博客入口**：支持一键创建空白草稿并进入编辑器，作为手写写作的入口。
- **导出与下载**：支持将单篇博客或整个系列的博客打包下载为 Markdown 格式（ZIP 压缩包）。
- **系列 PDF 异步导出（2026-06-03 更新）**：
  - 当用户在侧边栏触发“导出 PDF”时，系统必须先创建导出任务，再异步生成 PDF，并在任务完成后提供一次性的下载入口。
  - 导出产物必须通过受控下载接口返回；下载成功后立即删除共享目录中的文件，避免导出产物长期堆积。
- **直通 Obsidian 知识库**：支持一键将生成的单篇博客（附带标准 YAML Frontmatter）直接导出至本地 Docker 挂载的 Obsidian Vault 目录，实现个人知识库的无缝沉淀。
- **系列批量 Ingest（知识复利）**：支持一键将整个系列批量导入 Obsidian，并遵循 Karpathy LLM Wiki Pattern：系列作为 `sources/`，章节作为 `concepts/`，并自动抽取关键 `entities/` 与 `concepts/` 建立双向链接网络，同时更新 `wiki/index.md`、`wiki/log.md`、`wiki/hot.md`。

### 2.3 知识漫游复习
- **独立入口**：登录后，侧边栏必须提供“知识漫游复习”入口，进入独立主视图，不与博客生成页混在同一工作流中。
- **单步聚焦**：无论是知识复习还是博客生成，页面主区同一时间只展示当前步骤；其他步骤通过统一步骤条做预览或进度反馈，避免多个主任务区域同时平铺。
- **两种开始方式**：
  - 随机抽题：用户手动随机抽取一篇可复习文章，且“再抽一篇”在存在其它候选时应尽量换到不同文章。
  - 手动选文：用户可检索并选择知识库中的候选笔记开始复习。
- **两种训练模式**：
  - `light_recall`：更偏“你先讲，我再给提示”的轻提示复述。
  - `detailed_qa`：更偏逐轮追问与细致问答。
- **会话闭环**：
  - 创建 session 后，页面必须展示开场问题、当前文章的结构化复习快照、本轮目标、可请求提示、可提交回答、可主动结束训练。
  - `detailed_qa` 的追问必须基于当前文章内容推进，至少覆盖“主线问题 -> 关键概念/步骤 -> 场景/迁移解释”三类方向，而不是固定模板问答。
  - 用户每次提交回答后，系统必须明确返回“本轮判断、答到的点、漏掉的点、下一步建议”，让用户知道自己到底说对了多少。
  - 所有阶段反馈、提示、总结文案都必须使用中文，避免暴露底层模型的英文或调试信息。
  - 最近复习记录必须能看到刚完成的 session 摘要，便于用户复盘。
- **数据边界**：
  - 复习正文来源于 Obsidian `wiki/` 下的知识页面。
  - 业务状态、反馈结果与轮次记录持久化到 PostgreSQL，不直接把整篇原始笔记全文复制进数据库。

### 2.4 用户与授权
- **第三方登录**：支持通过 GitHub OAuth 进行授权登录。
- **普通注册/登录**：支持基于邮箱/密码的常规账号注册与登录。
- **安全防爆破机制**：注册和登录强制校验本地图形验证码防刷；连续 5 次密码错误将自动锁定账号 15 分钟，并在密码输入时提供实时强度校验提示。
- **Token 鉴权**：系统采用 JWT 机制进行用户身份的验证和路由拦截，支持“保持登录状态”签发长效 Token。
- **用户个人中心与仪表盘**：新增仪表盘展示用户消费的 Tokens 数量、预估费用（1元/1M Tokens）、文章总数、总字数，并以图表直观展示用户使用最高频的技术栈排行。允许用户修改用户名及上传头像。

## 3. 非功能性需求
- **并发与限流保护**：并发生成博客时，使用环境变量动态控制并发数（默认 10），最大上限可调至 50，以充分利用大模型的性能并防止 API 限流（429 错误）和服务器内存溢出。
- **Token 长度保护**：拥抱 DeepSeek V4 的 1M Token 上下文能力，向大模型发送的源内容字符数限制提升至 2,000,000 字符以内，以保证超大型项目的完整解析能力。
- **隐私与安全**：用户上传的文件仅在内存中处理，解析完成后必须立即销毁，绝不持久化存储物理文件实体。
- **容器化部署**：前端、后端和数据库必须通过 Docker Compose 一键启动（推荐 `docker compose --env-file backend/.env up -d --build`），确保本地与线上环境的完全一致性。前端由 Nginx 提供服务并代理后端 API，默认仅暴露 `http://localhost` 作为公开访问入口。
