- [x] GitFetcher 能够成功拉取公开仓库并正确过滤忽略文件夹，提取文本后临时目录被彻底删除。
- [x] 复杂度评估器能够在输入长文本时返回结构合理的 JSON 大纲（包含章节标题与摘要）。
- [x] 并发调度引擎能够根据大纲同时启动多个 LLM 请求，且无死锁或数据竞态问题。
- [x] 数据库 `blogs` 表中能正确保存系列博客（带有同一 `ParentID` 与正确的 `ChapterSort` 顺序）。
- [x] 接口能在生成系列博客时，通过 SSE 实时向前端反馈整体进度或某章节的生成流。